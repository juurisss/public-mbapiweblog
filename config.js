const botArgs = {
    host: 'us.mineberry.org',
    username: 'bot00421',
};

const password = 'pass'

const joincounter = 10
// This changes the amount of players it doesnt do /stat on when it just joined the bw lobby. 
// Change this to a higher number if the bot gets kicked when it joins a lobby

module.exports = { botArgs, password, joincounter };